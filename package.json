{
  "name": "codex-development-workflow",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "A file-backed Interactive and Night Shift workflow runner for Codex.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/joker01-01/night-mode.git"
  },
  "main": "dist/index.js",
  "bin": {
    "codex-workflow": "scripts/night-mode"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node test/run.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
